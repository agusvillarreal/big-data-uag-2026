{
 "cells": [
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "# Sample Data Analysis Notebook\n",
    "\n",
    "This is an example Jupyter notebook for data analysis with Docker."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Import common data science libraries\n",
    "import pandas as pd\n",
    "import numpy as np\n",
    "import matplotlib.pyplot as plt\n",
    "import seaborn as sns\n",
    "\n",
    "# Set plot style\n",
    "plt.style.use('seaborn-v0_8-whitegrid')\n",
    "%matplotlib inline"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Create sample data\n",
    "np.random.seed(42)\n",
    "data = {\n",
    "    'date': pd.date_range('2024-01-01', periods=100),\n",
    "    'sales': np.random.randint(100, 1000, 100),\n",
    "    'customers': np.random.randint(10, 100, 100),\n",
    "    'category': np.random.choice(['A', 'B', 'C'], 100)\n",
    "}\n",
    "df = pd.DataFrame(data)\n",
    "df.head()"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Basic statistics\n",
    "df.describe()"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Visualization: Sales over time\n",
    "plt.figure(figsize=(12, 4))\n",
    "plt.plot(df['date'], df['sales'])\n",
    "plt.title('Sales Over Time')\n",
    "plt.xlabel('Date')\n",
    "plt.ylabel('Sales')\n",
    "plt.tight_layout()\n",
    "plt.show()"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Sales by category\n",
    "plt.figure(figsize=(8, 5))\n",
    "df.groupby('category')['sales'].mean().plot(kind='bar')\n",
    "plt.title('Average Sales by Category')\n",
    "plt.ylabel('Average Sales')\n",
    "plt.tight_layout()\n",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "## Database Connection Example\n",
    "\n",
    "Connect to PostgreSQL database running in Docker."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Database connection (uncomment to use)\n",
    "# import os\n",
    "# from sqlalchemy import create_engine\n",
    "# \n",
    "# DATABASE_URL = os.environ.get('DATABASE_URL', 'postgresql://admin:secret@database:5432/myapp')\n",
    "# engine = create_engine(DATABASE_URL)\n",
    "# \n",
    "# # Query data from database\n",
    "# df_db = pd.read_sql('SELECT * FROM your_table LIMIT 100', engine)\n",
    "# df_db.head()"
   ]
  }
 ],
 "metadata": {
  "kernelspec": {
   "display_name": "Python 3",
   "language": "python",
   "name": "python3"
  },
  "language_info": {
   "name": "python",
   "version": "3.11.0"
  }
 },
 "nbformat": 4,
 "nbformat_minor": 4
}
