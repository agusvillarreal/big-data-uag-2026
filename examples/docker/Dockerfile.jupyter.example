# Example Dockerfile for Jupyter Notebook (Data Science)
# Copy this file to your project root and rename to "Dockerfile"

# Base image with Jupyter
FROM python:3.11-slim

# Set working directory inside the container
WORKDIR /notebooks

# Set environment variables
ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

# Install system dependencies for data science libraries
RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc \
    g++ \
    libpq-dev \
    && rm -rf /var/lib/apt/lists/*

# Install Jupyter and common data science packages
RUN pip install --no-cache-dir \
    jupyter \
    jupyterlab \
    pandas \
    numpy \
    matplotlib \
    seaborn \
    scikit-learn \
    sqlalchemy \
    psycopg2-binary

# Copy notebooks and data (optional)
COPY . .

# Expose Jupyter port
EXPOSE 8888

# Run Jupyter Lab with no token for development (use token in production!)
CMD ["jupyter", "lab", "--ip=0.0.0.0", "--port=8888", "--no-browser", "--allow-root", "--NotebookApp.token=''"]
